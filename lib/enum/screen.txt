return function(global)
    local screen_size_w, screen_size_h = 512, 512

    if global then
        SCREEN_SIZE_W, SCREEN_SIZE_H = screen_size_w, screen_size_h
    end

    return {
        SIZE_W = screen_size_w,
        SIZE_H = screen_size_h,
    }
end